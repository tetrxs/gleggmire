/**
 * Test setup for gleggmire.net
 *
 * Required dev dependencies (install before running tests):
 *   npm install -D vitest @vitejs/plugin-react
 *
 * Add to package.json scripts:
 *   "test": "vitest",
 *   "test:run": "vitest run"
 *
 * For integration tests against a real Supabase instance, set:
 *   TEST_SUPABASE_URL=<your-test-project-url>
 *   TEST_SUPABASE_KEY=<your-test-service-role-key>
 */
